# 🤖 Telegram Admin Bot

Мощный бот-администратор для управления Telegram группами с 12+ функциями.

## ✨ Функции

| # | Функция | Описание |
|---|---------|----------|
| 1 | **Бан/Разбан** | Полная блокировка пользователей |
| 2 | **Мут/Размут** | Временное ограничение отправки сообщений |
| 3 | **Система предупреждений** | Warn/Unwarn с автобаном при превышении лимита |
| 4 | **Очистка сообщений** | Массовое удаление сообщений |
| 5 | **Закрепление** | Pin/Unpin сообщений |
| 6 | **Приветствия** | Кастомные приветствия новых участников |
| 7 | **Антиспам** | Фильтр нежелательных слов |
| 8 | **Антифлуд** | Защита от флуда сообщениями |
| 9 | **Статистика** | Статистика чата и пользователей |
| 10 | **Медленный режим** | Slowmode для чата |
| 11 | **Кик** | Удаление пользователя (может вернуться) |
| 12 | **Логирование** | Логирование действий администраторов |

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/your-username/telegram-admin-bot.git
cd telegram-admin-bot
```

### 2. Создайте виртуальное окружение

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
LOG_CHANNEL_ID=-1001234567890
```

**Получение токена бота:**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

### 5. Запустите бота

```bash
python admin_bot.py
```

## 📝 Команды

### 👮 Модерация

| Команда | Описание | Пример |
|---------|----------|--------|
| `/ban` | Забанить пользователя | `/ban @user причина` |
| `/unban` | Разбанить пользователя | `/unban 123456789` |
| `/kick` | Кикнуть пользователя | `/kick @user` |
| `/mute` | Замутить (минуты) | `/mute 30 спам` |
| `/unmute` | Размутить | `/unmute @user` |

### ⚠️ Предупреждения

| Команда | Описание | Пример |
|---------|----------|--------|
| `/warn` | Выдать предупреждение | `/warn @user причина` |
| `/unwarn` | Снять предупреждение | `/unwarn @user` |
| `/warns` | Показать предупреждения | `/warns @user` |

### 📝 Управление сообщениями

| Команда | Описание | Пример |
|---------|----------|--------|
| `/clear` | Удалить сообщения | `/clear 50` |
| `/pin` | Закрепить сообщение | Ответ + `/pin` |
| `/unpin` | Открепить сообщение | `/unpin` |
| `/unpin_all` | Открепить все | `/unpin_all` |

### 👋 Приветствия

| Команда | Описание | Пример |
|---------|----------|--------|
| `/welcome` | Вкл/Выкл приветствия | `/welcome on` |
| `/setwelcome` | Установить текст | `/setwelcome Привет, {mention}!` |

**Переменные для приветствия:**
- `{mention}` - упоминание пользователя
- `{name}` - имя пользователя
- `{chat}` - название чата

### 🛡 Защита

| Команда | Описание | Пример |
|---------|----------|--------|
| `/antispam` | Вкл/Выкл антиспам | `/antispam on` |
| `/antiflood` | Настройка антифлуда | `/antiflood set 5 10` |
| `/addword` | Добавить запрещенное слово | `/addword спам` |
| `/delword` | Удалить слово | `/delword спам` |
| `/badwords` | Список слов | `/badwords` |
| `/slowmode` | Медленный режим | `/slowmode 60` |

### 📊 Информация

| Команда | Описание |
|---------|----------|
| `/stats` | Статистика чата |
| `/chatinfo` | Информация о чате |
| `/userinfo` | Информация о пользователе |
| `/help` | Список команд |

## 🐳 Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "admin_bot.py"]
```

```bash
docker build -t admin-bot .
docker run -d --env-file .env admin-bot
```

## 📁 Структура проекта

```
telegram-admin-bot/
├── admin_bot.py      # Основной код бота
├── requirements.txt  # Зависимости
├── .env.example      # Пример конфигурации
├── .env              # Конфигурация (создать)
├── README.md         # Документация
└── .gitignore        # Git ignore
```

## 🔧 Настройка прав бота

После добавления бота в группу, выдайте ему права администратора:
- ✅ Удалять сообщения
- ✅ Блокировать участников
- ✅ Закреплять сообщения
- ✅ Приглашать пользователей
- ✅ Изменять профиль чата

## 📄 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются!

---

⭐ Если проект был полезен, поставьте звезду на GitHub!
